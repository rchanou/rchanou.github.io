
        <div class="header">
            <img src="{{asset($images['header'])}}">
        </div>
        @yield('steps')
        {{--<div class="extras">
        Extras: {{link_to('#','Buy Gift Cards')}} - {{link_to('login','Login (TEST)')}}
        </div>--}}